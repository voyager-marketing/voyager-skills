{
  "mcpServers": {
    "wordpress": {
      "command": "wp",
      "args": [
        "--path={{WP_ROOT}}",
        "mcp-adapter",
        "serve",
        "--server=mcp-adapter-default-server",
        "--user=1"
      ]
    }
  }
}
